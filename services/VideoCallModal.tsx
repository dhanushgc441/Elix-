import React from 'react';

// This component is no longer used and has been cleared to remove the video call feature.
const VideoCallModal: React.FC = () => {
  return null;
};

export default VideoCallModal;
